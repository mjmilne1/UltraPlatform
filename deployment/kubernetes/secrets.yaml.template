# ============================================================================
# ANYA KUBERNETES SECRETS (TEMPLATE)
# ============================================================================
# WARNING: DO NOT COMMIT ACTUAL SECRETS TO VERSION CONTROL
# This is a template. Create actual secrets using kubectl or sealed-secrets

apiVersion: v1
kind: Secret
metadata:
  name: anya-secrets
  namespace: anya
type: Opaque
stringData:
  openai-api-key: "YOUR_OPENAI_API_KEY_HERE"
  postgres-user: "anya"
  postgres-password: "YOUR_SECURE_PASSWORD_HERE"
  jwt-secret: "YOUR_JWT_SECRET_HERE"

---
# To create from command line:
# kubectl create secret generic anya-secrets \
#   --from-literal=openai-api-key=YOUR_KEY \
#   --from-literal=postgres-password=YOUR_PASSWORD \
#   --from-literal=jwt-secret=YOUR_SECRET \
#   -n anya
